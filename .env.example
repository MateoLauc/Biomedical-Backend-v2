# Example environment variables for biomedical-backend
# Copy to .env and fill values before running locally or deploying.

# General
NODE_ENV=development
PORT=4000

# Database
# DATABASE_URL=postgres://user:pass@localhost:5432/biomedical

# CORS - comma separated list of allowed origins for cross-site requests.
# For staging, include: https://biomedical-v2.vercel.app
# For production, set your frontend origin(s) (no wildcard) and make sure credentials are allowed.
CORS_ORIGINS=http://localhost:3000,https://biomedical-v2.vercel.app

# JWT secrets (use strong random values in production)
# JWT_ACCESS_SECRET=your_access_secret_here
# JWT_REFRESH_SECRET=your_refresh_secret_here

# Mail / Cloudinary / Paystack - optional example placeholders
# MAILTRAP_API_KEY=
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# PAYSTACK_SECRET_KEY=
# PAYSTACK_WEBHOOK_SECRET=

