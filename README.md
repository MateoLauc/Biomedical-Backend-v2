# Biomedical Backend

Production-grade backend scaffold for a medication e-commerce (Express + TypeScript), designed for **Vercel serverless** and **Neon Postgres**.

## Local setup

- Copy `env.example` to `.env` and fill values (create `.env` manually).
- Install dependencies and run:

```bash
npm run dev
```

## Endpoints

- `GET /health`

